mcp>=1.2.0
uvicorn>=0.30.0
starlette>=0.37.0
python-dotenv>=1.0.0
# PyObjC bindings to macOS EventKit. Backs every tool — we go directly to
# the framework instead of shelling out to osascript/JXA per call. With ~200
# reminders, that drops `list_lists` from 10s to 60ms.
pyobjc-framework-EventKit>=10.0
